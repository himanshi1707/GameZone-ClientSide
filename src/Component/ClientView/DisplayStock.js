import React from 'react';
import { useSelector } from 'react-redux';

export default function DisplayStock(props){
    var data=useSelector()

    return(
        <div>
            
        </div>
    )
}